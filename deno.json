{
	"version": "1.0.0",
	"description": "discord bot for singularity",
	"main": "app.ts",
  "author": "Scyzemic",
  "license": "MIT",
  "tasks": {
    "start": "deno run app.ts",
    "register": "deno run --allow-env --allow-net --env-file=.env.local commands.ts",
    "dev": "deno run --allow-net --allow-read --allow-env --env-file=.env.local --watch app.ts"
  },
  "imports": {
    "@std/assert": "jsr:@std/assert@1"
  }
}
